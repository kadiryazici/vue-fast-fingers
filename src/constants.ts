export enum WordStatus {
  Wrong,
  Correct,
  None,
}

export enum ColorCSSVars {
  Accent = 'var(--accent)',
  Black = 'var(--black)',
  White = 'var(--white)',
  Correct = 'var(--correct)',
  Wrong = 'var(--wrong)',
}
